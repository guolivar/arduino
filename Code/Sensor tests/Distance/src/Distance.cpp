#include "Arduino.h"


void setup(){
    Serial.begin(9600);
}

// takes the array finds the lenght
int normalize(int * nums){
  return sum / size;
}

void loop() {
  //Serial.println(analogRead(2));
}
